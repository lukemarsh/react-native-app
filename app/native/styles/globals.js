import {
  StyleSheet
} from 'react-native';

export const globalStyles = StyleSheet.create({
  wrapper: {
    flex: 1
  },
  darkGreen: {
    color: '#96be2d'
  },
  bold: {
    fontWeight: 'bold'
  }
});
